#!/bin/bash
#Crear un programa que pida una lista de numeros y devuelva aquellos que son multiplos del primero

#Crear un programa que pida por linea de comandos un numero y que muestre la tabla de multiplicar de ese numero.

#Crear un programa que llame al script anterior pasandole una lista de numeros.

#Crear un script que reciva un nombre de grupo y devuelva todos los usuarios que pertenecen a ese grupo

#Crear un script que reciva una lista de grupos y devuelva todos los usuarios de esos grupos

#Crear un script que reciva una lista de grupos y devuelva los que no pertenezcan a ninguno

#Crear un script que reciba una cadena y diga si es palindromo, que pida por parametro y si no por read

#Crear un script que devuelva el proceso que mas archivos tiene abiertos "lsof"
